# Jarvis Studio v0.1 Acceptance

| # | Requirement | Evidence |
|---:|---|---|
| 1 | Import `trace.jsonl` | `POST /api/runs/import-jsonl`, Runs import modal, `fixtures/data-analysis/sample-trace.jsonl`, parser tests |
| 2 | View Runs list | Runs Registry page and `GET /api/runs` |
| 3 | View complete Trace Tree | Trace Viewer page and `GET /api/runs/:runId/spans` |
| 4 | View LLM Call detail | Span Detail shows output plus complete model/token/performance metadata |
| 5 | View Tool Call detail | Tool Console shows reason, arguments, permission, result, stdout/stderr refs, latency, and context injection |
| 6 | View Context Snapshot | Context Inspector shows final Prompt, source segments, token ratio, truncation, and compression |
| 7 | View multi-turn conversation | Conversation Replay page and `GET /api/sessions/:sessionId/turns` |
| 8 | Manage Prompt versions | Prompt Lab create/new-version/test endpoints and UI |
| 9 | Import Eval Case | YAML import endpoint/UI, built-in YAML fixture, import test |
| 10 | Run rule scoring | Eval Bench batch runner and rule scorer tests |
| 11 | Compare two Prompt versions | Compare page and `POST /api/compare/prompts` |
| 12 | Output Release Gate conclusion | Compare page and `POST /api/release-gate` output PASS/BLOCK |

## Verification Commands

```bash
npm run seed
npm run typecheck
npm test
npm run build
./script/build_and_run.sh
```

Browser smoke verification covered JSONL import, Trace/LLM/Tool details, Context inspection, two-turn replay, Prompt test/version creation, batch Eval, Prompt comparison, Release Gate, and the disabled-by-default Tool replay safety rule.
