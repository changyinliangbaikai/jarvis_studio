# Excel Data Analysis

1. Use `xlsx.inspect` before drawing conclusions.
2. Preserve the real sheet names, headers, row counts, missing-value counts, and numeric summaries.
3. Use `filesystem.write` to prepare a bounded Python analysis script.
4. Use `python.run` to generate `output/analysis_report.md`.
5. The final answer must mention 数据概况、异常发现、营销建议 and the generated artifact.
